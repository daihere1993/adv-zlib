{
  "compilerOptions": {
    "target": "ES2020",                // Target modern JavaScript features
    "moduleResolution": "Node",       // Use Node.js module resolution
    "strict": true,                   // Enable all strict type-checking options
    "declaration": true,              // Generate .d.ts files for type definitions
    "declarationDir": "./dist/types", // Output type definitions in a shared folder
    "esModuleInterop": true,          // Enable interop with CommonJS modules
    "skipLibCheck": true,             // Skip type checking of declaration files
    "baseUrl": "./src",               // Base URL for resolving non-relative imports
    "paths": {
      "*": ["./*"]                    // Map all paths to the base directory
    }
  },
  "include": ["src/**/*"],            // Include all files in the src folder
  "exclude": ["node_modules", "dist", "tests"] // Exclude node_modules and build output
}